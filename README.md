# Note-Card-Stack-App
An interactive **Note / Call Management UI** built using **HTML, CSS, and Vanilla JavaScript**.   The application allows users to create call notes, store them in **localStorage**, and view them in a **stacked card interface** with navigation controls.
